# {{TITLE}} — Agent entry

Project: `{{PROJECT_ID}}`  
Core: `{{CORE_VERSION}}`
Required capability: `$research-os`

## Start and resume

1. Invoke `$research-os` with this directory as the Vault root.
2. Run `research-os doctor --project .` and `research-os project setup-status --project .`.
3. Run `research-os session context --project .`.
4. Read current `authoritativeSources` and only required files for this Action. Follow checkpoint history only when needed.

Project objective: `PROJECT.md.foreground_objective`. Current controls and next step: `{{ACTIVE_PLAN}}`. History: its checkpoint reference and domain records.

Missing capabilities, invalid authority or incomplete setup block dependent writes; report the exact issue and continue unaffected authorized work.

## Work and finish

- Keep one canonical source per fact; generated views are rebuildable.
- Configure necessary checks and task-specific attempt limits before execution. Stop after passing; record reasoned revisions after exhaustion.
- Reuse actual user approval within its operations, paths and resources. Never fabricate acceptance or bypass platform permissions.
- Preflight exact scope and claim execution resources. Avoid overlapping writers; consume only accepted Artifact versions.
- Record blockers, causes, known times and resolutions in the Action.
- Execution completion does not approve science or delivery. On actual acceptance, update task status and next step.
- Validate changed authority, checkpoint factual work, and report uncommitted project records.
